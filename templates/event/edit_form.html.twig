<form id="editEventForm" action="{{ path('app_event_edit', {'id': event.id}) }}" method="POST">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
</form>
